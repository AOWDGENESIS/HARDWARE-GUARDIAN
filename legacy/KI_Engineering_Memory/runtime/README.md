# Runtime

This folder is intentionally local-only.

The updater writes current machine and tool facts here. Do not commit these files to a public repository.
The root `.gitignore` excludes generated runtime data.
